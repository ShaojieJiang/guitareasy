{
  "name": "guitareasy-mcp-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync-seed-scores": "node scripts/sync-seed-scores.mjs",
    "sync-player-assets": "node scripts/sync-player-assets.mjs",
    "sync": "npm run sync-seed-scores && npm run sync-player-assets",
    "predev": "npm run sync",
    "dev": "wrangler dev --local-upstream localhost:8787",
    "prebuild": "npm run sync",
    "build": "tsc --noEmit",
    "predeploy": "npm run sync",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.10.3",
    "@modelcontextprotocol/ext-apps": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "agents": "^0.23.0",
    "worker-mailer": "^1.2.1",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "typescript": "~6.0.2",
    "wrangler": "^4.134.0"
  }
}
